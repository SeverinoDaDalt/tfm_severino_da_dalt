\documentclass{beamer}
\usetheme{Madrid}  % Pittsburgh, Madrid
\usecolortheme{default}

\usepackage{amsmath, amssymb}
\usepackage{mathtools}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{fit, positioning, shapes.geometric}
\usepackage{booktabs} % Added for table styling
\usetikzlibrary{fit, decorations.pathreplacing}
\usetikzlibrary{overlay-beamer-styles}
\usepackage{bbm}


\usepackage{algorithm}
\usepackage{algorithmicx}
\usepackage{algpseudocode}
\makeatletter
\AddToHook{env/algorithmic/before}{\def\@currentcounter{ALG@line}}
\makeatother
\usepackage[nameinlink]{cleveref}  % this is for a lot of things, not only algorithms
\crefalias{ALG@line}{line}
\makeatletter
\renewcommand{\ALG@beginalgorithmic}{\fontsize{6.5}{4}\selectfont}
\makeatother
\algrenewcommand\alglinenumber[1]{\fontsize{6.5}{4}\selectfont #1:}


\newcommand{\Parcurly}[1]{\left\{ #1 \right\}}
\newcommand{\parcurly}[1]{\{ #1 \}}
\newcommand{\bparcurly}[1]{\bigl\{ #1 \bigr\}}
\newcommand{\bbparcurly}[1]{\Bigl\{ #1 \Bigr\}}

\newcommand{\Parround}[1]{\left( #1 \right)}
\newcommand{\parround}[1]{( #1 )}
\newcommand{\bparround}[1]{\bigl( #1 \bigr)}
\newcommand{\bbparround}[1]{\Bigl( #1 \Bigr)}

\newcommand{\Parsquared}[1]{\left[ #1 \right]}
\newcommand{\parsquared}[1]{[ #1 ]}
\newcommand{\bparsquared}[1]{\bigl[ #1 \bigr]}

\newcommand{\Parstraight}[1]{\left| #1 \right|}
\newcommand{\parstraight}[1]{| #1 |}
\newcommand{\bparstraight}[1]{\bigl| #1 \bigr|}

\newcommand{\Partriangle}[1]{\left< #1 \right>}
\newcommand{\partriangle}[1]{< #1 >}

\newcommand{\ceil}[1]{\left\lceil #1 \right\rceil}
\newcommand{\floor}[1]{\left\lfloor #1 \right\rfloor}

\newcommand{\naturals}{\mathbb{N}}


\bibliographystyle{plain}


\title[Regularity for Stable Graphs]{The Regularity Lemma for Stable Graphs and its Applications in Property Testing}
\author{Severino Da Dalt}
\institute[]{Supervised by Lluís Vena Cros}
\date{October 15, 2025}

\begin{document}

\nocite{*} % needed for bibliography

% Title page
\begin{frame}
    \titlepage
\end{frame}

% ---
% Table of contents
\begin{frame}{Table of contents}
    \begin{enumerate}
        \item Introduction to Szemerédi's Regularity Lemma
        \item Stable Graphs and the Stable Regularity Lemma
        \item Sketch of the Proof of the Stable Regularity Lemma
        \item Property Testing of $H$-freeness in Stable Graphs
        \item Conclusion and Future Work
    \end{enumerate}
\end{frame}

% ---
% Regularity
\begin{frame}{Introduction to Szemerédi's Regularity Lemma}
    \visible<1->{
        The idea of the SzRL is that all graphs can be partitioned in such a way that most pairs of parts
        behave in a quasi-random way, a property which is formalized as \emph{regularity}.
    }

    \begin{definition}<2->
        Given $\epsilon > 0$ and a graph $G$, a pair of (not necessarily disjoint) subsets of vertices $A, B \subseteq G$
        is said to be \emph{$\epsilon$-regular} if for all $A' \subseteq A$ and $B' \subseteq B$ such that
        $|A'| \geq \epsilon |A|$ and $|B'| \geq \epsilon |B|$, we have
        \[
            |d(A', B') - d(A, B)| \leq \epsilon.
        \]
    \end{definition}
\end{frame}

% Regular partitions
\begin{frame}{Introduction to Szemerédi's Regularity Lemma}
    When most of the pairs satisfy this property, we say that the partition is \emph{regular}.

    \begin{definition}
        Let $G$ be a graph and let $\epsilon > 0$.
        An \emph{$\epsilon$-regular partition} of $G$ is a partition of its vertex set into $k$ parts
        $\parcurly{A_1, \ldots, A_k}$ with remainder set $B$ such that:
        \begin{itemize}
            \item $|B| \leq \epsilon |G|$, and may be empty.
            \item All but at most $\epsilon k^2$ of the pairs $(A_i, A_j)$ with $1 \leq i < j \leq k$ are
                $\epsilon$-regular.
        \end{itemize}
    \end{definition}
\end{frame}

% SzRL
\begin{frame}{Introduction to Szemerédi's Regularity Lemma}
    \begin{theorem}<1->[Szemerédi's Regularity Lemma, \textit{Szemerédi '78}]
        For every $\epsilon > 0$ and every positive integer $m$, there exists a positive integer $M = M(\epsilon, m)$
        such that every graph with at least $m$ vertices admits an $\epsilon$-regular partition
        $\parcurly{A_1, \ldots, A_k}$ and remainder $B$ with $m \leq k \leq M$, with $|A_1| = \dots = |A_k|$.
    \end{theorem}

    \visible<2->{
        Crucially, the theorem proves that the bound on the number of parts $M$ does not grow with the size of the graph,
        but only depends on $\epsilon$ (and $m$).
        % Since the partition of singletons is always regular.
    }
    % The SzRL sees extensive use in combinatorics, number theory, and computer science.
\end{frame}

% Limitations of SzRL
\begin{frame}{Introduction to Szemerédi's Regularity Lemma}
    However, the lemma has two major drawbacks:
    \begin{enumerate}
        \item The bound on the number of parts $M$ is a \emph{tower-type} function of $\epsilon$,
            i.e. a power-tower of height polynomial in $1/\epsilon$.
            This is unavoidable, as shown by \textit{Gowers '97}.
        \item The partition may contain some irregular pairs.
            This is also unavoidable, and the half-graph $H_n$ is an example.
    \end{enumerate}
\end{frame}

% ---

% half-graph
\begin{frame}{Stable Graphs and the Stable Regularity Lemma}
    \emph{Stable} graphs are those that do not contain a \emph{half-graph} as a bi-induced subgraph.
    \input{pictures/half-graph_presentation}
    % comment that this graph has bounded VC, which will be relevant later.
\end{frame}

% non-k-order property
\begin{frame}{Stable Graphs and the Stable Regularity Lemma}
    \begin{definition}<1-> \label{def:k_order_property}
        Let $G$ be a graph.
        We say that $G$ has the \emph{$k$-order property} if there exist two sequences of vertices
        $\Partriangle{a_i \mid i \in \parcurly{1, \dots, k}}$ and $\Partriangle{b_i \mid i \in \parcurly{1, \dots, k}}$ such that
        for all $i,j \leq k$, $a_i R b_j$ if and only if $i \geq j$.
        Otherwise, we say that $G$ has the \emph{non-$k$-order property} or that $G$ is \emph{$k$-stable}.
    \end{definition}

    \visible<2->{
        The two sequences need not to be disjoint, and any combination of edges and non-edges may appear between
        the vertices of each sequence.
    }
\end{frame}

% other non-k-orders
\begin{frame}{Stable Graphs and the Stable Regularity Lemma}
    \input{pictures/other_k-order_presentation}
\end{frame}

\begin{frame}{Stable Graphs and the Stable Regularity Lemma}
    \begin{theorem}[Stable Regularity Lemma,~\textit{Malliaris-Shelah '14}] \label{thm:B}
        Each $k$-stable graph $G$ admits an equitable partition of its vertex set with at most
        ${c(k)\cdot(1/\epsilon)^{2^{k+1}}}$ parts such that ALL pairs of parts
        are $\epsilon$-regular and have density either less than $\epsilon$ or greater than $1 - \epsilon$.
    \end{theorem}
    \visible<2->{
        The fact that the pairs are either almost empty or almost full is not a core property of stable graphs,
        and is in fact proven in~\textit{Fox-Pach-Suk '19} and~\textit{Lovász-Szegedy '10} to be satisfied by a
        larger class of graphs, the graphs with bounded \emph{VC-dimension}.
        However, such graphs still require irregular pairs, something that stable graphs completely avoid.
    }
    % def VC-dimension somewhere?
\end{frame}

% ---

% goodness
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \begin{definition}<1->[Good,~\textit{Malliaris-Shelah '14}] \label{def:epsilon_good}
        Let $G$ be a finite graph with the non-$k_*$-property.
        We say that $A \subseteq G$ is $\epsilon$-\emph{good} when for every $b \in G$ the truth value
        $t = t(b, A) \in \parcurly{0, 1}$ satisfies
        $\parstraight{\overline{B}_{A,b}} = \parstraight{\parcurly{a\in A \mid aRb \not\equiv t}} < \epsilon |A|$.
    \end{definition}

    \begin{center}
        \scalebox{0.6}{
        \begin{tikzpicture}
            \visible<2-> {
%                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a1) at (3.56, 3.55) {$a_1$};
%                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a2) at (3.6, 5.93) {$a_2$};
%                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a3) at (5, 5) {$a_3$};
%                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a4) at (2.2, 5.05) {$a_4$};
%                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a5) at (5.16, 3.25) {$a_5$};
%                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a6) at (1.52, 3.39) {$a_6$};
%                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a7) at (2.86, 1.87) {$a_7$};
%                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a8) at (4.66, 1.63) {$a_8$};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a1) at (3.56, 3.55) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a2) at (3.6, 5.93) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a3) at (5, 5) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a4) at (2.2, 5.05) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a5) at (5.16, 3.25) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a6) at (1.52, 3.39) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a7) at (2.86, 1.87) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a8) at (4.66, 1.63) {};
                \node (A) [draw, thick, circle, inner sep=4pt, fit=(a1) (a2) (a3) (a4) (a5) (a6) (a7) (a8), label=45:\Large $A$] {};
            }
            \visible<3-> {
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt, label=45:\Large $b$] (b) at (10.81, 3.7) {};
            }
            \visible<4-6> {
                \node (BAb) [draw, thick, fill=orange!70!red, fill opacity=0.5, circle, inner sep=2pt, fit=(a2) (a3), label=160:\Large $\overline{B}_{A,b}$] {};
            }
            \visible<7-> {
                \node (BAb) [draw, dashed, thick, fill=orange!70!red, fill opacity=0.5, circle, inner sep=2pt, fit=(a2) (a3)] {};
                \node (BAb) [draw, thick, fill=orange!70!red, fill opacity=0.5, circle, inner sep=5pt, fit=(a2) (a3), label=160:\Large $\overline{B}_{A,b}$] {};
            }
            \visible<5-> {
                \draw[thick] (a2) -- (b);
                \draw[thick] (a3) -- (b);
                \draw[dashed] (a1) -- (b);
                \draw[dashed] (a4) -- (b);
                \draw[dashed] (a5) -- (b);
                \draw[dashed] (a6) -- (b);
                \draw[dashed] (a7) -- (b);
                \draw[dashed] (a8) -- (b);
                \node[below=5pt of b.south] {\Large $t(A,b) = 0$};
            }
            \visible<4> {
                \draw[dashed] (a2) -- (b);
                \draw[dashed] (a3) -- (b);
                \draw[thick] (a1) -- (b);
                \draw[thick] (a4) -- (b);
                \draw[thick] (a5) -- (b);
                \draw[thick] (a6) -- (b);
                \draw[thick] (a7) -- (b);
                \draw[thick] (a8) -- (b);
                \node[below=5pt of b.south] {\Large $t(A,b) = 1$};
            }
            \visible<2-> {
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a1) at (3.56, 3.55) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a2) at (3.6, 5.93) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a3) at (5, 5) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a4) at (2.2, 5.05) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a5) at (5.16, 3.25) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a6) at (1.52, 3.39) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a7) at (2.86, 1.87) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a8) at (4.66, 1.63) {};
            }
            \visible<6-> {
                \node[below=5pt of A.south] {\Large $A$ is \textbf{$\epsilon$-good}};
            }
            \visible<7-> {
                \node[below=22pt of A.south] {\Large For any $\epsilon \leq \epsilon'$, $A$ is also \textbf{$\epsilon'$-good}};
            }
        \end{tikzpicture}}
    \end{center}
\end{frame}

% excellence
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \begin{definition}<1->[Excellent,~\textit{Malliaris-Shelah '14}] \label{def:epsilon_excellent}
        Let $G$ be a finite graph with the non-$k_*$-property.
        We say that $A \subseteq G$ is $(\epsilon, \zeta)$-\emph{excellent} when $A$ is $\epsilon$-good and, if $B$ is
        $\zeta$-good, then the truth value $t = t(B,A)$ satisfies $\parstraight{\parcurly{a \in A \mid t(a,B) \neq t(A,B)}} < \epsilon |A|$.
        In particular, we say $A$ is $\epsilon$-\emph{excellent} if $A$ is $(\epsilon, \epsilon)$-excellent.
    \end{definition}

    \begin{center}
        \scalebox{0.5}{
        \begin{tikzpicture}
            \visible<2-5> {
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a2) at (3.6, 5.93) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a3) at (5, 5) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a5) at (5.16, 3.25) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a6) at (1.52, 3.39) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a7) at (2.86, 1.87) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a8) at (4.66, 1.63) {};
            }
            \visible<2-> {
                \node (A) [draw, thick, circle, inner sep=4pt, fit=(a1) (a2) (a3) (a4) (a5) (a6) (a7) (a8), label=45:\Large $A$] {};
            }
            \visible<2-7> {
                \node[below=5pt of A.south] {\Large $A$ is $\epsilon$-good};
            }
            \visible<2-3> {
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a1) at (3.56, 3.55) {};
            }
            \visible<2-4> {
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a4) at (2.2, 5.05) {};
            }
            \visible<3-> {
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (b1) at (10.6, 3.2) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (b2) at (10.8, 4.5) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (b3) at (12.3, 5.4) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (b4) at (11.8, 3.8) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (b5) at (11.56, 2.2) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (b6) at (12.8, 2.5) {};
                \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (b7) at (13.2, 4.4) {};
                \node (B) [draw, thick, circle, inner sep=4pt, fit=(b1) (b2) (b3) (b4) (b5) (b6) (b7), label=25:\Large $B$] {};
                \node[below=5pt of B.south] {\Large $B$ is $\zeta$-good};
            }
            \visible<4-5> {
                \draw[thick] (b1) -- (a1);
                \draw[dashed] (b2) -- (a1);
                \draw[dashed] (b3) -- (a1);
                \draw[dashed] (b4) -- (a1);
                \draw[dashed] (b5) -- (a1);
                \draw[dashed] (b6) -- (a1);
                \draw[dashed] (b7) -- (a1);
                \node[circle, dashed, semithick, draw, fill=gray!5, minimum size=0.3cm, inner sep=1pt] (a1) at (3.56, 3.55) {};
            }
            \visible<5> {
                \draw[thick] (b1) -- (a4);
                \draw[thick] (b2) -- (a4);
                \draw[thick] (b3) -- (a4);
                \draw[thick] (b4) -- (a4);
                \draw[thick] (b5) -- (a4);
                \draw[thick] (b6) -- (a4);
                \draw[dashed] (b7) -- (a4);
                \node[circle, thick, draw, fill=gray!75, minimum size=0.3cm, inner sep=1pt] (a4) at (2.2, 5.05) {};
            }
            \visible<7-8> {
                \node (BAb) [draw, thick, fill=orange!70!red, fill opacity=0.5, circle, inner sep=2pt, fit=(a2) (a4)] {};
            }
            \visible<9-> {
                \node (BAb) [draw, dashed, thick, fill=orange!70!red, fill opacity=0.5, circle, inner sep=2pt, fit=(a2) (a4)] {};
                \node (BAb) [draw, thick, fill=orange!70!red, fill opacity=0.5, circle, inner sep=5pt, fit=(a2) (a4)] {};
            }
            \visible<6-> {
                \node[circle, dashed, semithick, draw, fill=gray!5, minimum size=0.3cm, inner sep=1pt] (a1) at (3.56, 3.55) {};
                \node[circle, thick, draw, fill=gray!75, minimum size=0.3cm, inner sep=1pt] (a2) at (3.6, 5.93) {};
                \node[circle, dashed, semithick, draw, fill=gray!5, minimum size=0.3cm, inner sep=1pt] (a3) at (5, 5) {};
                \node[circle, thick, draw, fill=gray!75, minimum size=0.3cm, inner sep=1pt] (a4) at (2.2, 5.05) {};
                \node[circle, dashed, semithick, draw, fill=gray!5, minimum size=0.3cm, inner sep=1pt] (a5) at (5.16, 3.25) {};
                \node[circle, dashed, semithick, draw, fill=gray!5, minimum size=0.3cm, inner sep=1pt] (a6) at (1.52, 3.39) {};
                \node[circle, dashed, semithick, draw, fill=gray!5, minimum size=0.3cm, inner sep=1pt] (a7) at (2.86, 1.87) {};
                \node[circle, dashed, semithick, draw, fill=gray!5, minimum size=0.3cm, inner sep=1pt] (a8) at (4.66, 1.63) {};
            }
            \visible<8-> {
                \node[circle, thick, draw, minimum size=1.7cm, inner sep=1pt] () at (14.6, 6.6) {};
                \node[circle, thick, draw, minimum size=0.6cm, inner sep=1pt] () at (14.2, 1.4) {};
                \node[below=5pt of A.south] {\Large $A$ is \textbf{$(\epsilon, \zeta)$-excellent}};
            }
            \visible<8> {
                \node[circle, thick, draw, minimum size=2cm, inner sep=1pt] () at (15.8, 3.2) {};
                \node[circle, thick, draw, minimum size=1.5cm, inner sep=1pt] () at (9, 1.6) {};
            }
            \visible<9-> {
                \node[circle, dashed, semithick, draw, minimum size=2cm, inner sep=1pt] () at (15.8, 3.2) {};
                \node[circle, dashed, semithick, draw, minimum size=1.5cm, inner sep=1pt] () at (9, 1.6) {};
            }
            \visible<9-> {
                \node[below=22pt of A.south] {\Large For any $\epsilon \leq \epsilon'$ and $\zeta \geq \zeta'$,
                    $A$ is also \textbf{$(\epsilon', \zeta')$-excellent}};
            }
        \end{tikzpicture}}
    \end{center}
\end{frame}

% example
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    % 1
    \visible<1->{
        \input{pictures/non-monotonic_example_presentation}
    }
    % - A is 1/5-good but B is not
    % - A is 1/5-excellent
    % - A is not 2/5-excellent, because B is 2/5-good but half of A has truth value 0 wrt B, and the other half 1
    % - this last fact also introduces a discrepancy in the original, as it is assumed that \epsilon-excellence is
    %   monotonic, which is not the case
\end{frame}

% excellence implies regularity and homogeneity
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    % Now one may ask:
    \visible<1-> {
        \textbf{Question:} Why do we care about excellence?
    }

    \visible<2-> {
        \textbf{Answer:} A pair of excellent sets form a regular pair which is either almost full or almost empty.
    }

    \begin{lemma}<2->
        For any $\epsilon_0, \epsilon \in \parround{0, \frac{1}{2}}$ such that $\epsilon_0 \leq \frac{\epsilon^2}{2}$,
        and for any two $\epsilon_0$-excellent sets $A, B$, we have that:
        \begin{enumerate}
            \item $\parround{A,B}$ is $\epsilon$-regular.
            \item If $A' \in \parsquared{A}^{\geq \epsilon \parstraight{A}} and $
                $B' \in \parsquared{B}^{\geq \epsilon \parstraight{B}}$, then $d \parround{A',B'} < \epsilon$ or
                $d \parround{A',B'} \geq 1 - \epsilon$.
        \end{enumerate}
    \end{lemma}
\end{frame}

% k-trees
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \begin{definition}[$k$-tree]<1-> \label{def:k-tree}
        A \emph{$k$-tree} in $G$ is an ordered pair $H = (\overline{c},\overline{b})$ comprising two (not necessarily
        disjoint) sequences:
        \begin{itemize}
            \item $\overline{c} = \parcurly{ c_\eta \in G \mid \eta \in \parcurly{0,1}^{<k} }$, the set of \emph{nodes} and
            \item $\overline{b} = \parcurly{ b_\rho \in G \mid \rho \in \parcurly{0,1}^k }$, the set of \emph{branches},
        \end{itemize}
        satisfying that for all $\eta \in \parcurly{0,1}^{<k}$ and $\rho \in \parcurly{0,1}^k$,
        if for some $\ell \in \parcurly{0, 1}$ we have $\eta \frown \Partriangle{\ell} \triangleleft \rho$, then
        $b_\rho R c_\eta \equiv \ell$.
    \end{definition}

    \begin{definition}[Tree bound]<1-> \label{def:tree_bound}
        Suppose $G$ is a finite graph.
        We denote the \emph{tree bound} $k_{**} = k_{**}(G)$ as the minimal positive integer such that there is no
        $k_{**}$-tree $H = (\overline{c},\overline{b})$ in $G$.
    \end{definition}
\end{frame}

% example of k-tree
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \input{pictures/k-tree_presentation}
\end{frame}

% relation between k-trees and non-k-order property: k-tree in 2^k-order
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \input{pictures/half-graph_implies_k-tree_presentation}
\end{frame}

% relation between k-trees and non-k-order property: 2^k-order in k-tree
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \begin{theorem} \label{thm:tree_implies_order}
        \visible<1-> {
            If a graph $G$ has the $2^{k_{**}}$-order property, then the tree bound of $G$ is at least $k_{**} + 1$.
            On the other hand, if a graph $G$ has tree bound at least $k_{**} = 2^{k_*+1}-1$, then it has the $k_*$-order
            property.
        }
    \end{theorem}
\end{frame}

% existence of excellent sets
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    % 1
    \begin{lemma}[Existence of excellent subsets,~\textit{Malliaris-Shelah '14}]<1-> \label{lem:existance_of_excellent_subsets}
        Let $G$ be a finite graph with the non-$k_{*}$-order property.
        Let $\zeta \leq \frac{1}{2^{k_{**}}}$, $\epsilon \in \parround{0, \frac{1}{2}}$.
        Then, for every $A \subseteq G$ with $|A| \geq \frac{1}{\epsilon^{k_{**}}}$ there exists an $(\epsilon, \zeta)$-excellent
        subset $A' \subseteq A$ such that $|A'| \geq \epsilon^{k_{**}-1} |A|$.
    \end{lemma}

%    % 2
%    \visible<2-> {
%        TO DO
%    }
\end{frame}

% existence of excellent sets: proof sketch
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \input{pictures/k-tree_for_existence_presentation}
\end{frame}

% we can actually ensure size is in a given sequence of sizes with not much extra work
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \begin{lemma}[Existence of excellent subsets,~\textit{Malliaris-Shelah '14}]
        Let $G$ be a finite graph with the non-$k_{*}$-order property.
        Let $\zeta < \frac{1}{2^{k_{**}}}$, $\epsilon \in \parround{0, \frac{1}{2}}$.
        Let $\Partriangle{m_\ell \mid \ell \in \parcurly{0, \dots, k_{**}}}$ be a decreasing sequence of natural numbers such that
        $\epsilon m_{\ell} \geq m_{\ell+1}$ for all $\ell \in \parcurly{0, \dots, k_{**}-1}$ and $m_{k_{**}} \geq 1$.
        Then, for every $A \subseteq G$ with $|A| \geq m_0$ there exists
        $\parround{\frac{m_{\ell+1}}{m_{\ell}}, \zeta}$-excellent subset $A' \subseteq A$ such that $|A'| = m_\ell$ for
        some $\ell \in \parcurly{0, \dots, k_{**}-1}$.
    \end{lemma}
\end{frame}

% building uneven partition
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \begin{lemma} \label{lem:existance_of_excellent_partition}
        Let $G$ be a finite graph with the non-$k_{*}$-order property.
        Let $\epsilon \in \parround{0, \frac{1}{2}}$ and $\epsilon' \leq \frac{1}{2^{k_{**}}}$.
        Let $A \subseteq G$ such that $|A| = n \geq m_0$.
        Let $\Partriangle{m_\ell \mid \ell \in \parcurly{0, \dots, k_{**}}}$ be a decreasing sequence of natural numbers such that
        $\epsilon m_{\ell} \geq m_{\ell+1}$ for all $\ell \in \parcurly{0, \dots, k_{**}-1}$ and $m_{k_{**}} \geq 1$.
        Denote $m_* \coloneqq m_0$ and $m_{**} \coloneqq m_{k_{**}}$.
        Then, there is a partition $\overline{A} = \Partriangle{A_j \mid j \in \parcurly{1, \dots, j(*)}}$ with remainder
        $B = A \setminus \bigcup_{j < j(*)} A_j$ such that:
        \begin{enumerate}
            \item For all $j \in \parcurly{1, \dots, j(*)}$, $|A_j| \in \Partriangle{m_\ell \mid \ell \in \parcurly{0, \dots, k_{**}-1}}$.
            \item For all $i \neq j \in \parcurly{1, \dots, j(*)}$, $A_i \cap A_j = \emptyset$.
            \item For all $j \in \parcurly{1, \dots, j(*)}$, $A_j$ is $\parround{\epsilon, \epsilon'}$-excellent.
            \item $|B| < m_*$.
        \end{enumerate}
    \end{lemma}
\end{frame}

% subsets of excellent sets are still excellent
\begin{frame}
    \visible<1-> {
        \textbf{Question:} Are subsets of excellent sets still excellent?
        \newline
    }

    \visible<2-> {
        \textbf{Answer:} With high probability, yes, but with a small loss in excellence.
        \newline
    }

    \visible<3-> {
        The proof of this uses:
        \begin{itemize}
            \item The Sauer-Shelah Lemma to bound the number of possible exceptional sets $\overline{B}_{A,b}$ in $A$.
            \item Chvátal's bound on the tail of the hypergeometric distribution.
            \item A double-counting argument on the exceptional vertices of a random subset, using the fact that the number of
                exceptional edges between an excellent set and a good set is very small.
        \end{itemize}
    }
\end{frame}

% partition excellent part into equally-sized excellent parts
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    Thus, random subsets of excellent sets are (slightly less) excellent with high probability,
    and we can use this fact with a Union Bound argument to prove the following result.

    \begin{lemma}
        Let $G$ be a finite graph with the non-$k_{*}$-order property.
        Then, for all $\zeta \in \parround{0, \frac{1}{2}}$, $\zeta' < \zeta$, $r \geq 1$ and for all
        $\epsilon < \epsilon'$ small enough ($\leq \epsilon_1(\zeta, \zeta')$ from the previous point) there exists
        $N = N\parround{k_{*}, \zeta', r}$ such that: if $|A| = n > N$, $r$ divides $n$ and $A$ is
        $\parround{\epsilon, \epsilon'}$-excellent, then there exists a partition into $r$ disjoint pieces of equal
        size, each of which is $\parround{\epsilon + \zeta, \epsilon'}$-excellent.
    \end{lemma}
\end{frame}

% partition excellent part into equally-sized excellent parts
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    Applying this result to the previously obtained uneven partition, and carefully choosing some parameters,
    we obtain an even partition into excellent parts.
    \begin{lemma}
        Let $G$ be a finite graph with the non-$k_{*}$-order property.
        Let $\epsilon'$ and $\epsilon$ be two real numbers such that
        $\epsilon' \leq \min \parcurly{\frac{1}{5}, \frac{1}{2^{k_{**}}}}$ and $\epsilon \leq \frac{1}{4k} \epsilon'$ for some $k > 1$.
        Also, let $m_*$, $m_{**}$ and $q$ be natural numbers such that $q \geq \ceil{\frac{1}{\epsilon}}$,
        $m_{**} > \frac{N \parround{k, k_*, \epsilon', \frac{m_*}{m_{**}}}}{q}$ and $m_* \coloneqq q^{k_{**}} m_{**}$.
        Then, for any $A \subseteq G$ with $|A| = n \geq m_*$ there exists a partition
        $\overline{A} = \Partriangle{A_i \mid i \in \parcurly{1, \dots, i(*)}}$ with remainder $B = A \setminus \bigcup \overline{A}$ such that:
        \begin{enumerate}
            \item $i(*) \leq \frac{n}{m_{**}}$.
            \item For all $i \in \parcurly{1, \dots, i(*)}$, $|A_i| = m_{**}$.
            \item For all $i \in \parcurly{1, \dots, i(*)}$, $A_i$ is $\parround{\frac{\epsilon'}{k}, \epsilon'}$-excellent.
            \item$|B| < m_*$.
        \end{enumerate}
    \end{lemma}
    But we still have some remainder.
\end{frame}

% getting rid of remainder
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    This can be addressed by uniformly distributing the vertices of the remainder among the parts.
    This, again, slightly reduces the excellence of each part.
    \begin{lemma}
        Under the same condition of the previous lemma, we can get a
        partition $\overline{A} = \Partriangle{A_i \mid i \in \parcurly{1, \dots, i(*)}}$ with no remainder, such that:
        \begin{enumerate}
            \item For all $i, j \in \parcurly{1, \dots, i(*)}$, $\parstraight{\parstraight{A_i}- \parstraight{A_j}} \leq 1$.
            \item For all $i, j \in \parcurly{1, \dots, i(*)}$, $A_i \cap A_j = \emptyset$.
            \item For all $i \in \parcurly{1, \dots, i(*)}$, $A_i$ is $\parround{\epsilon'', \epsilon'}$-excellent, for some $\epsilon''$
                satisfying
                \[
                    0 < \epsilon'' \leq \frac{\frac{\epsilon'}{k} m_{**} + \ceil{\frac{m_*}{i(*)}}}{m_{**} + \ceil{\frac{m_*}{i(*)}}}.
                \]
            \item $A = \bigcup \overline{A}$.
        \end{enumerate}
    \end{lemma}
\end{frame}

% recap
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    By carefully choosing parameters, we can rephrase the result to only depend on the stability constant $k_*$,
    the desired excellence $\epsilon$ and the minimum partition size $m$.
    \begin{theorem}
        Let $k_*$ be given.
        Then, for all $\epsilon \leq \min \parcurly{\frac{1}{5}, \frac{1}{2^{k_{**}}}}$ and $m > 1$, there is $M = M\parround{\epsilon, m, k_*}$
        and $N = N\parround{\epsilon, m, k_*}$ such that, for every finite graph $G$ with the non-$k_{*}$-order property, and
        every $A \subseteq G$ with $|A| \geq N$, there exists a partition $\overline{A} = \Partriangle{A_i \mid i \in \parcurly{1, \dots, i(*)}}$
        of $A$, such that:
        \begin{enumerate}
            \item The number of parts is bounded by
                $m \leq i(*) \leq M \coloneqq \max \parcurly{\ceil{\frac{12}{\epsilon}}^{k_{**}+1}, 4m}$.
            \item For all $i, j \in \parcurly{1, \dots, i(*)}$, $\parstraight{\parstraight{A_i}- \parstraight{A_j}} \leq 1$.
            \item For all $i \in \parcurly{1, \dots, i(*)}$, $A_i$ is $\epsilon$-excellent.
        \end{enumerate}
    \end{theorem}
\end{frame}

% final result
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \begin{theorem}[Stable Regularity Lemma,~\textit{Malliaris-Shelah '14}]
        For every $k_* \in \naturals$ and
        $\epsilon \in \parround{0, \frac{1}{2}}$ and $m > 1$, there exist $N = N\parround{\epsilon, m, k_*}$
        and $M = M\parround{\epsilon, m, k_*}$ such that, for every finite graph $G$ with the non-$k_{*}$-order property,
        and every $A \subseteq G$ with $|A| \geq N$, the following holds.
        There exists $m < \ell < M$ and a partition
        $\overline{A} = \Partriangle{A_i \mid i \in \parcurly{1, \dots, \ell}}$ of $A$ such that each $A_i$ is
        $\frac{\epsilon^2}{2}$-excellent and, for every (not necessarily distinct) $i, j \in \parcurly{1, \dots, \ell}$,
        \begin{enumerate}
            \item $\parstraight{\parstraight{A_i}- \parstraight{A_j}} \leq 1$, i.e. the partition is equitable.
            \item $\parround{A_i, A_j}$ is $\epsilon$-regular, and moreover if
                $B_i \in \parsquared{A_i}^{\geq \epsilon \parstraight{A_i}}$ and $B_j \in \parsquared{A_j}^{\geq \epsilon \parstraight{A_j}}$,
                then either $d\parround{B_i, B_j} < \epsilon$ or $d\parround{B_i, B_j} \geq 1 - \epsilon$.
            \item If $\epsilon \leq \min \parcurly{\frac{1}{5}, \frac{1}{2^{k_{**}}}}$, then
                $M \leq \max \parcurly{\ceil{\frac{12}{\epsilon}}^{k_{**}+1}, 4m}$.
        \end{enumerate}
    \end{theorem}
\end{frame}

% ---

% what is property testing and H-freeness:1
\begin{frame}{Property Testing of $H$-freeness in Stable Graphs}
    \begin{definition}<2->
        We say that a graph $G$ is \emph{$\epsilon$-far} from satisfying a graph property $\mathcal{P}$ if no adding or
        removing of up to $\epsilon {|G| \choose 2}$ edges in $G$ results in the graph satisfying the property.
    \end{definition}

    \begin{definition}<2->
        An \emph{$\epsilon$-test} $\mathcal{A}$ deciding a graphs property $\mathcal{P}$ with query complexity
        $q(n)$ is a randomized algorithm that, on input graph $G$ of size $n$,
        satisfies the following.
        \begin{enumerate}
            \item If $G \in \mathcal{P}$, then $\mathbb{P}\parround{\mathcal{A} \text{ accepts } G} \geq \frac{2}{3}$.
            \item If $G$ is $\epsilon$-far from satisfying $\mathcal{P}$,
                then $\mathbb{P}\parround{\mathcal{A} \text{ rejects } G} \geq \frac{2}{3}$.
        \end{enumerate}
        The query complexity $q(n)$ is the maximum number of queries the algorithm can make, and (in our case)
        a query discerns whether a desired pair of vertices in the input graph $G$ is adjacent or not.
    \end{definition}
\end{frame}

% what is property testing and H-freeness:2
\begin{frame}{Property Testing of $H$-freeness in Stable Graphs}
    \begin{definition}<1->
        We say that a property $\mathcal{P}$ is \emph{testable} if there exists an $\epsilon$-test deciding $\mathcal{P}$
        with a constant query-complexity with respect to the size of the input graph, that is, it only depends on the
        parameter $\epsilon$.
    \end{definition}

    \visible<2-> {
        A classic example of a testable property is $H$-freeness, for a fixed graph $H$, i.e. the property of not
        containing $H$ as an induced subgraph.
    }

    \visible<3-> {
        \textit{Alon-Fischer-Krivelevich-Szegedy '00} proved that $H$-freeness is testable for every $H$.
    }\visible<4-> {Later, \textit{Alon-Shapira '08}
        generalize this result to any hereditary property.% Properties closed under vertex deletion.
    }

    \begin{theorem}[Alon \& Shapira Theorem]<4->
        Every hereditary graph property is testable (with one-sided error).
    \end{theorem}
\end{frame}

% unavoidability and abundance
\begin{frame}{Property Testing of $H$-freeness in Stable Graphs}
    The main theoretical result we require to build our tester is that if a graph is \emph{far} from being $H$-free,
    then it contains many copies of $H$.

    \begin{definition} \label{def:unavoidable}
        A graph $H$ is \emph{$\gamma$-unavoidable} in a graph $G$ if no adding or removing of up to $\gamma {|G| \choose 2}$
        edges in $G$ results in $H$ not appearing as an induced subgraph of $G$.
    \end{definition}

    \begin{definition} \label{def:abundant}
        A graph $H$ is \emph{$\eta$-abundant} in a graph $G$ if $G$ contains at least $\eta |G|^{|H|}$
        induced copies of $H$.
    \end{definition}
\end{frame}

% unavoidability implies abundance:1
\begin{frame}{Property Testing of $H$-freeness in Stable Graphs}
    \begin{lemma}[\textit{Alon-Fischer-Krivelevich-Szegedy '00}, \say{Counting Lemma}]
        For every real number $\delta \in \parround{0, 1}$ and integer $\ell > 0$ there exist $\epsilon = \epsilon \parround{\delta, \ell}$ and
        $\eta = \eta \parround{\delta, \ell}$ satisfying the following property:

        Let $H$ be a graph with vertices $v_1, \dots,v_\ell$ and let $V_1, \dots, V_\ell$ be an $\ell$-tuple of (not necessarily disjoint)
        sets of vertices of a graph $G$ such that for every $1 \leq i < i' \leq \ell$, the pair $\parround{V_i, V_{i'}}$
        is $\epsilon$-regular, with density at least $\delta$ if $v_i v_{i'}$ is an edge of $H$, and at most $1 - \delta$
        if $v_i v_{i'}$ is not an edge of $H$.
        Then, at least $\eta \prod_{i=1}^\ell \parstraight{V_i}$ of $\ell$-tuples $w_1 \in V_1, \dots, w_\ell \in V_\ell$
        span induced copies of $H$ where $w_i$ plays the role of $v_i$.
    \end{lemma}
\end{frame}

% unavoidability implies abundance:2
\begin{frame}{Property Testing of $H$-freeness in Stable Graphs}
    \begin{theorem} \label{thm:property_testing_with_stable_partitions}
        For every $k_*, \gamma, \ell$ there is a $\eta(k_*, \gamma, \ell)$ such that if $H$ is a graph with $\ell$
        vertices, $G$ has the non-$k_*$-order property and $H$ is $\gamma$-unavoidable in $G$, then $H$ is
        $\eta$-abundant in $G$.
    \end{theorem}
\end{frame}

% unavoidability implies abundance:3 (proof sketch)
\begin{frame}{Property Testing of $H$-freeness in Stable Graphs}
    \centering
    \begin{tikzpicture}
        \def\radius{1.5cm}
        \foreach \i in {1,...,6} {
            \node[circle, thick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a\i) at ({60 + 360/6 * (\i - 1)}:\radius) {};
        }
        \draw[thick] (a1) -- (a2);
        \draw[thick] (a1) -- (a3);
        \draw[thick] (a1) -- (a4);
        \draw[dashed, black!40] (a1) -- (a5);
        \draw[dashed, black!40] (a1) -- (a6);
        \draw[thick] (a2) -- (a3);
        \draw[thick] (a2) -- (a4);
        \draw[dashed, black!40] (a2) -- (a5);
        \draw[dashed, black!40] (a2) -- (a6);
        \draw[thick] (a3) -- (a4);
        \draw[dashed, black!40] (a3) -- (a5);
        \draw[dashed, black!40] (a3) -- (a6);
        \draw[thick] (a4) -- (a5);
        \draw[thick] (a4) -- (a6);
        \draw[dashed, black!40] (a5) -- (a6);
        \node at (0, -2) {$H$};
    \end{tikzpicture}
\end{frame}

% unavoidability implies abundance:3 (proof sketch)
\begin{frame}{Property Testing of $H$-freeness in Stable Graphs}
    \centering
    \begin{tikzpicture}
        \visible<1,2,5> {
            \node at (-1.5, 5) {$\underline{G}$};
        }
        \visible<3,4> {
            \node at (-1.5, 5) {$\underline{G}'$};
        }
        \visible<1-> {
            \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a1) at (0, 0) {$a_1$};
            \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a2) at (-1.414, 1.414) {$a_2$};
            \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a3) at (-1.061, 0.354) {$a_3$};
            \node (A) [draw, thick, circle, inner sep=5pt, fit=(a1) (a2) (a3), label=135:$A$] {};
            \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (b1) at (5, 1.414) {$b_1$};
            \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (b2) at (3.586, 0) {$b_2$};
            \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (b3) at (4.646, 0.354) {$b_3$};
            \node (A) [draw, thick, circle, inner sep=5pt, fit=(b1) (b2) (b3), label=45:$B$] {};
            \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (c1) at (0.7925, 4.33) {$c_1$};
            \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (c2) at (2.7925, 4.33) {$c_2$};
            \node[circle, semithick, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (c3) at (1.7925, 4.83) {$c_3$};
            \node (A) [draw, thick, circle, inner sep=5pt, fit=(c1) (c2) (c3), label=45:$C$] {};
            \draw[semithick] (a1) -- (a2) -- (a3) -- (a1);
            \draw[semithick] (b1) -- (b2) -- (b3) -- (b1);
            \draw[dashed, black!40] (c1) -- (c2) -- (c3) -- (c1);
            \draw[semithick] (a1) -- (b1);
            \draw[semithick] (a1) -- (b2);
            \draw[semithick] (a1) -- (b3);
            \draw[semithick] (a2) -- (b1);
            \draw[semithick] (a2) -- (b2);
            \draw[semithick] (a2) -- (b3);
        }
        \visible<1,5> {
            \draw[dashed, black!40] (a3) -- (b1);
        }
        \visible<1-> {
            \draw[semithick] (a3) -- (b2);
            \draw[semithick] (a3) -- (b3);
            \draw[dashed, black!40] (b1) -- (c1);
            \draw[dashed, black!40] (b1) -- (c2);
        }
        \visible<1,5> {
            \draw[semithick] (b1) -- (c3);
        }
        \visible<1-> {
            \draw[dashed, black!40] (b2) -- (c1);
            \draw[dashed, black!40] (b2) -- (c2);
            \draw[dashed, black!40] (b2) -- (c3);
            \draw[dashed, black!40] (b3) -- (c1);
            \draw[dashed, black!40] (b3) -- (c2);
            \draw[dashed, black!40] (b3) -- (c3);
            \draw[semithick] (c1) -- (a1);
            \draw[semithick] (c1) -- (a2);
            \draw[semithick] (c1) -- (a3);
            \draw[semithick] (c2) -- (a1);
            \draw[semithick] (c2) -- (a2);
            \draw[semithick] (c2) -- (a3);
            \draw[semithick] (c3) -- (a1);
        }
        \visible<1,5> {
            \draw[dashed, black!40] (c3) -- (a2);
        }
        \visible<1-> {
            \draw[semithick] (c3) -- (a3);
        }
        \visible<2> {
            \draw[semithick, dashed, orange!70!red] (a3) -- (b1);
            \draw[semithick, orange!70!red] (b1) -- (c3);
            \draw[semithick, dashed, orange!70!red] (c3) -- (a2);
        }
        \visible<3,4> {
            \draw[semithick] (a3) -- (b1);
            \draw[dashed, black!40] (b1) -- (c3);
            \draw[semithick] (c3) -- (a2);
        }
        \visible<4-> {
            \draw[semithick, orange!70!red] (a1) -- (a2);
            \draw[semithick, orange!70!red] (a1) -- (b2);
            \draw[semithick, orange!70!red] (a1) -- (b3);
            \draw[semithick, orange!70!red] (a2) -- (b2);
            \draw[semithick, orange!70!red] (a2) -- (b3);
            \draw[semithick, orange!70!red] (b2) -- (b3);
            \draw[semithick, orange!70!red] (a2) -- (c1);
            \draw[semithick, orange!70!red] (a2) -- (c2);
            \node[circle, semithick, text=orange!70!red, draw=orange!70!red, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a1) at (0, 0) {$a_1$};
            \node[circle, semithick, text=orange!70!red, draw=orange!70!red, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (a2) at (-1.414, 1.414) {$a_2$};
            \node[circle, semithick, text=orange!70!red, draw=orange!70!red, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (b2) at (3.586, 0) {$b_2$};
            \node[circle, semithick, text=orange!70!red, draw=orange!70!red, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (b3) at (4.646, 0.354) {$b_3$};
            \node[circle, semithick, text=orange!70!red, draw=orange!70!red, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (c1) at (0.7925, 4.33) {$c_1$};
            \node[circle, semithick, text=orange!70!red, draw=orange!70!red, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (c2) at (2.7925, 4.33) {$c_2$};
        }
    \end{tikzpicture}
\end{frame}

% the algorithm
\begin{frame}{Property Testing of $H$-freeness in Stable Graphs}
    \begin{algorithm}[H]
        \caption{$\epsilon$-test $\mathcal{A}$ for deciding $H$-freeness}
        \label{alg:h-freeness_tester}
        \begin{algorithmic}[1]
            \Require a graph $H$ of size $\ell$, a natural number $k_*$ and a real number $\epsilon > 0$.
            \Require an oracle $\mathcal{O}$ accepting queries of whether two vertices of a graph $G$ are adjacent.
                The graph $G$ has the non-$k_*$-order property, and only its size $n$ is known.
            \State $t \leftarrow \ell \log\parround{\frac{2}{3}} /
                \log\parround{1 - \eta(k_*, \epsilon, \ell)}$ \Comment{
                    Compute sample size $t$}
            \If{$n < \ell$} \Comment{Check if $G$ is large enough to contain $H$}
                \State $\mathcal{A}$ \emph{rejects} $G$ \label{line:G_smaller_then_H}
            \ElsIf{$n < t$} \Comment{Check if $G$ is small enough to query all edges}
                \State query all pairs of vertices of $G$ to $\mathcal{O}$
                \If{$\exists v_{i_1}, \dots, v_{i_\ell} \in G$ such that
                        $\parcurly{v_{i_1}, \dots, v_{i_\ell}}$ induces a copy of $H$ in $G$}
                    \State $\mathcal{A}$ \emph{accepts} $G$ \label{line:G_small_enough_found_H}
                \Else
                    \State $\mathcal{A}$ \emph{rejects} $G$ \label{line:G_small_enough_not_found_H}
                \EndIf
            \Else \label{line:random_sampling} \Comment{Sample $t$ vertices uniformly at random, without repetitions}
                \State $S \leftarrow \emptyset$
                \While{$i \leq t$}
                    \State $s_{i} \sim G$
                    \While{$s_i \in S$} \Comment{Repeat until a new vertex is sampled}
                        \State $s_{i} \sim G$
                    \EndWhile
                    \State $S \leftarrow S \cup \parcurly{s_i}$
                \EndWhile
                \State query all pairs of vertices of $S$ to $\mathcal{O}$
                \If{$\exists v_1, \dots, v_\ell \in S$ such that
                        $\parcurly{v_1, \dots, v_\ell}$ induces a copy of $H$ in $G$}
                    \State $\mathcal{A}$ \emph{accepts} $G$ \label{line:found_H}
                \Else
                    \State $\mathcal{A}$ \emph{rejects} $G$ \label{line:not_found_H}
                \EndIf
            \EndIf
        \end{algorithmic}
    \end{algorithm}
\end{frame}

% the bounds
\begin{frame}{Property Testing of $H$-freeness in Stable Graphs}
    \footnotesize
    \visible<1-> {
        A lower bound for the abundance $\eta$ only depending on the unavoidability parameter $\gamma$, the stability $k_*$, and
        and graph $H$'s size $\ell$, is:
%        \[
%            \eta \geq \Parround{\frac{1}{2}}^{\frac{\ell^3 + 5\ell -6}{6}}
%                \cdot (1 - \gamma)^{\frac{\ell(\ell-1)}{2}}
%                \cdot \Parround{\frac{1}{24} \min\Parcurly{\sqrt{\gamma},
%                    \Parround{\frac{1}{2}}^{\frac{\ell(\ell-1)}{2}} (1 - \gamma)^{\ell-1}}}^{\ell \Parround{2^{k_*+1}-1}}.
%        \]
        \[
            \eta \geq \bparround{\gamma \cdot (1 - \gamma)} ^ {c \cdot \ell^3 \cdot 2^{k_*}}.
        \]
    }
    \visible<2-> {
        The resulting query complexity of the algorithm $\mathcal{A}$ can bounded by
%        \[
%            q \leq {t \choose 2}
%              \leq \Parround{ \frac{\ell \log\parround{\frac{2}{3}}}
%                   {\log\parround{1 - \eta(k_*, \epsilon, \ell)}}}^2.
%        \]
        \[
            q \approx \ell^2 \Parround{\frac{1}{{\gamma \cdot (1 - \gamma)}}}^{c \cdot \ell^3 \cdot 2^{k_*}}.
        \]
    }
\end{frame}

% ---

% limitations of the algorithm
\begin{frame}{Conclusion and Future Work}
    \footnotesize
    \begin{itemize}
        \item<1-> We provided a simplified combinatorial proof of the \emph{Stable Regularity Lemma},
            developed a unified notational framework.
        \item<2-> We designed an efficient \emph{property testing algorithm} for $H$-freeness in stable graphs.
            Our algorithm leverages the most powerful feature of the Stable Regularity Lemma: its partitions
            have \emph{no irregular pairs}.
            This provides a uniquely clean structure that is ideal for property testing.
        \item<3-> A comparison with the Strong Regularity Lemma (for the broader class of graphs with bounded VC-dimension)
            reveals a potential suboptimality.
            \begin{itemize}
                \item \footnotesize Strong Lemma Bound: $(1/\epsilon)^{c \cdot d^2}$
                \item \footnotesize Stable Lemma Bound: $(1/\epsilon)^{c \cdot 2^k}$
            \end{itemize}
            This raises a key question: Can the bound for the Stable Regularity Lemma be improved to match the polynomial
            exponent, i.e., to $(1/\epsilon)^{c \cdot k^2}$?
        \item<4-> The powerful structure guaranteed by stable regularity opens up new research avenues, including:
            \begin{itemize}
                \item \footnotesize Testing for forbidden subgraphs ($H_n$) that \emph{grow in size} with the input graph.
                \item \footnotesize Moving beyond freeness testing to \emph{subgraph counting} and estimation.
            \end{itemize}
    \end{itemize}
\end{frame}

%% lines of research with Lluis
%\begin{frame}{Conclusion and Future Work}
%\end{frame}
%
%\begin{frame}{Conclusion and Future Work}
%\end{frame}
%
%\begin{frame}{Conclusion and Future Work}
%\end{frame}

% ---

% references
\begin{frame}[allowframebreaks]{References}
    \bibliography{sections/presentation_bibliography}
\end{frame}

% Thanks
\begin{frame}
    \centering
    \Huge{\textbf{Thank you!}}
    \vspace{1em}

    \Large{You can now rate the presentation.
    Was it \emph{good}, \emph{excellent} or \emph{exceptional}?}
\end{frame}


% Thanks
\begin{frame}{Appendix}
\end{frame}% refining to an equitable partition
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \visible<1-> {
        \textbf{Question:} Are subsets of excellent sets still excellent?
    }

    \visible<1-> {
        \textbf{Answer:} With high probability, yes, but with a small loss in excellence.
    }

    \begin{lemma}<2->
        Let $G$ be a finite graph with the non-$k_{*}$-order property.
        Then, for every $\epsilon \in \parround{0, \frac{1}{2}}$,
        $\zeta \in \parround{0, \frac{1}{2} - \epsilon}$, $\xi \in \parround{0, 1}$ and
        $m \geq \frac{1}{\zeta^2}\parround{k_* \log{\frac{1}{\zeta^2}k_*} - \log{\xi}}$,
        if $A \subseteq G$ is an $\epsilon$-good subset of size $n \geq m$,
        then a subset $A' \subseteq A$ of size $m$, sampled uniformly at random, is $(\epsilon + \zeta)$-good with probability $1-\xi$.
    \end{lemma}

    \begin{lemma}<3->
        Let $G$ be a finite graph with the non-$k_{*}$-order property.
        Then, for every $\zeta \in \parcurly{0, \frac{1}{2}}$ and $\zeta' < \zeta$, there is
        $\epsilon_1 = \epsilon_1(\zeta, \zeta')$ such that for every $\epsilon < \epsilon' \leq \epsilon_1$, if
        \begin{itemize}
            \item $A \subseteq G$ is $\parround{\epsilon, \epsilon'}$-excellent and
            \item $A' \subseteq A$ is $\parround{\epsilon + \zeta'}$-good,
        \end{itemize}
        then, $A'$ is $\parround{\epsilon + \zeta, \epsilon'}$-excellent.
    \end{lemma}
\end{frame}

% proof sketch of good subsets in good sets: 1
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \begin{center}
        \begin{tikzpicture}
            \centering
            \visible<3-> {
                \node[draw, circle, fill=orange!70!red, fill opacity=0.5, minimum size=3cm] (A') at (1,0) {};
            }
            \visible<3-> {
                \node at (A'.center) [below right] {$A'$};
            }
            \visible<2-> {
                \node[draw, circle, minimum size=3cm] (BAb) at (0,1) {};
                \node at (BAb.center) [above left] {$\overline{B}_{A,b}$};
            }
            \visible<1-> {
                \node (A) [draw, thick, circle, inner sep=4pt, fit=(A') (BAb), label=45:$A$] {};
                \node[circle, draw, fill=gray!20, minimum size=0.3cm, inner sep=1pt] (b) at (5,0.5) {$b$};
            }
            \visible<2-> {
                \draw[dashed, black!40] (b) -- (BAb.north east);
                \draw[dashed, black!40] (b) -- (BAb.south east);
            }
            \visible<4-> {
                \draw[draw=none] (BAb.center) -- node[pos=0.55] (M) {$\overline{B}_{A,b} \cap A'$} (A'.center);
            }
        \end{tikzpicture}
    \end{center}
\end{frame}

% proof sketch of good subsets in good sets: 2
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \visible<1-> {
        The probability of failure is small for each $b$:
        \[
            \mathbb{P}\parround{X_{A,b,A'}} \leq \mathbb{P}\Parround{\parstraight{\overline{B}_{A,b} \cap A'} \geq
                \bbparround{\frac{|\overline{B}_{A,b}|}{|A|} + \zeta} |A'|} \leq e^{-2\zeta^2 |A'|}
        \]
    }
    \visible<2-> {
        Too many $b$'s?
    }
    \begin{lemma}<3-> \label{lem:k_order_property_bounds_BAbs}
        Let $G$ be a graph with the (disjoint) non-$k$-order property.
        Then, for any finite non-trivial $A' \subseteq G$,
        \[
            \parstraight{\parcurly{\overline{B}_{A',b} \mid b \in G}} \leq |A'|^k.
        \]
    \end{lemma}
\end{frame}

% proof sketch of good subsets in good sets: 3
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \visible<1-> {
        Given the random variable
        \[
            X = \sum_{\text{bad } \overline{B}_{A,b}} \mathbbm{1}_{X_{A,b,A'}},
        \]
    }
    \visible<2-> {
        we can compute its expectation
        \[
            \mathbb{E}[X] = \sum_{\text{bad } \overline{B}_{A,b}} \mathbb{E}[\mathbbm{1}_{X_{A,b,A'}}]
                = \sum_{\text{bad } \overline{B}_{A,b}} \mathbb{P}\parround{X_{A,b,A'}}
                \leq \sum_{\text{bad } \overline{B}_{A,b}} e^{-2\zeta^2 m},
        \]
    }
    \visible<3-> {
        and by the First Moment Method
        \[
            \mathbb{P}\parround{X \geq 1} \leq \mathbb{E}[X] \leq m^{k_*} \cdot e^{-2\zeta^2 m} \leq \xi.
        \]
    }
\end{frame}

% proof sketch of excellent subsets in excellent sets: 1
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    \begin{lemma}<1->
        Let $G$ be a finite graph with the non-$k_{*}$-order property.
        Then, for every $\zeta \in \parcurly{0, \frac{1}{2}}$ and $\zeta' < \zeta$, there is
        $\epsilon_1 = \epsilon_1(\zeta, \zeta')$ such that for every $\epsilon < \epsilon' \leq \epsilon_1$, if
        \begin{itemize}
            \item $A \subseteq G$ is $\parround{\epsilon, \epsilon'}$-excellent and
            \item $A' \subseteq A$ is $\parround{\epsilon + \zeta'}$-good,
        \end{itemize}
        then, $A'$ is $\parround{\epsilon + \zeta, \epsilon'}$-excellent.
    \end{lemma}

    \textbf{Proof idea:} given a good set $B$, the number of exceptional
    edges between $A$ and $B$, and thus between $A'$ and $B$, is small, since $A$ is $(\epsilon, \epsilon')$-excellent.
    Also, since $B$ is good, each vertex in $A'$ either has few or many exceptional edges with $B$.
    These two facts together imply that the number of vertices in $A'$ that do not behave as the majority is small.
\end{frame}

% partition excellent part into equally-sized excellent parts
\begin{frame}{Sketch of the Proof of the Stable Regularity Lemma}
    Now, we know that random subsets of excellent sets are (slightly less) excellent with high probability.
    We can use this fact with a simple Union Bound argument to prove the following result.

    \begin{lemma}
        Let $G$ be a finite graph with the non-$k_{*}$-order property.
        Then, for all $\zeta \in \parround{0, \frac{1}{2}}$, $\zeta' < \zeta$, $r \geq 1$ and for all
        $\epsilon < \epsilon'$ small enough ($\leq \epsilon_1(\zeta, \zeta')$ from the previous point) there exists
        $N = N\parround{k_{*}, \zeta', r}$ such that: if $|A| = n > N$, $r$ divides $n$ and $A$ is
        $\parround{\epsilon, \epsilon'}$-excellent, then there exists a partition into $r$ disjoint pieces of equal
        size, each of which is $\parround{\epsilon + \zeta, \epsilon'}$-excellent.
    \end{lemma}
\end{frame}

\end{document}